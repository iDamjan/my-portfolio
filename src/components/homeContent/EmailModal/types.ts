export interface inputTypes {
  name: string;
  email: string;
  subject: string;
  message: string;
}

export interface inputTouchedStateTypes {
  name: boolean;
  email: boolean;
  subject: boolean;
  message: boolean;
}
